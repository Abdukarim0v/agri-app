apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-agri-app.fullname" . }}
  labels:
    {{- include "helm-agri-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "helm-agri-app.selectorLabels" . | nindent 4 }}
